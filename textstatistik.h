#pragma once

#include <string>
//#include <map>
//#include <set>
//#include <vector>

// text aus datei einlesen
// std::vector<std::string> readFile(std::string path);

// h�ufigkeit der buchstaben bestimmen
// std::map<char, int> letterFreq(std::vector<std::string>& words);

// h�ufigkeit der w�rter bestimmen
// std::set<std::string> wordFreq(std::vector<std::string>& words, unsigned int n);

// tabelle ausgeben
void printStatistic(std::string& path, unsigned int minLength, unsigned int maxLength);